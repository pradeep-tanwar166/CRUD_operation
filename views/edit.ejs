<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
   
</head>
<body>
   
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <h1 class="text-3xl tracking-tighter mb-3  ">Update User</h1>
        <div class="nav mb-10">
            <a class="text-blue-500" href="/read">Read Users</a>
        </div>
        <form action="/update/<%=user._id %>" method="post" >
            <input type="text" class="px-5 py-3 border-2 border-zinc-800 rounded lg bg-transparent outline-none " name="name" placeholder="Name" value="<%= user.name %>">
            <input type="text" class="px-5 py-3 border-2 border-zinc-800 rounded lg bg-transparent outline-none " name="email" placeholder="email" value="<%= user.email %>">
            <input type="text" class="px-5 py-3 border-2 border-zinc-800 rounded lg bg-transparent outline-none " name="image" placeholder="img-url" value="<%= user.image%>">
            <input class="px-5 py-2 bg-yellow-500 text-white rounded-lg ml-3" type="submit" value="update User">
        </form>
    </div>
</body>
</html>